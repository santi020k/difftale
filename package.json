{
  "name": "@santi020k/difftale-root",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "lint": "pnpm run lint:repo && turbo run lint",
    "lint:fix": "pnpm run lint:repo:fix && turbo run lint:fix",
    "lint:repo": "eslint . --no-warn-ignored --max-warnings=0",
    "lint:repo:fix": "eslint . --fix --no-warn-ignored --max-warnings=0",
    "ok": "pnpm install --frozen-lockfile && pnpm run build && pnpm run test && pnpm run lint && pnpm run typecheck",
    "package:extension": "pnpm --filter vscode-difftale package",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish",
    "sandbox:setup": "node scripts/setup-vscode-sandbox.mjs",
    "sandbox:reset": "node scripts/setup-vscode-sandbox.mjs --reset",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "typecheck": "turbo run typecheck",
    "prepare": "husky",
    "pre-push": "pnpm run ok",
    "commit": "commitprompt"
  },
  "devDependencies": {
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@santi020k/commitprompt": "catalog:",
    "@santi020k/eslint-config-astro": "catalog:",
    "@santi020k/eslint-config-basic": "catalog:",
    "@santi020k/eslint-config-extensions": "catalog:",
    "@santi020k/eslint-config-formats": "catalog:",
    "@santi020k/eslint-config-libraries": "catalog:",
    "@santi020k/eslint-config-testing": "catalog:",
    "@santi020k/eslint-config-tools": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "husky": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:"
  },
  "engines": {
    "node": "^22.22.3 || ^24.16.0 || >=26.3.0"
  },
  "packageManager": "pnpm@10.34.3"
}
